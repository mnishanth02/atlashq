# Leave empty for same-origin requests. In dev, Vite proxies `/api` to the local API
# (see `vite.config.ts`) so cookies stay same-origin; in production, Caddy serves the
# web app and API behind the same origin, so this stays empty there too.
VITE_API_BASE_URL=
VITE_APP_NAME=AtlasHQ

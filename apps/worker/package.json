{
  "name": "@atlashq/worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "biome check .",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "start": "node dist/main.js",
    "health": "node dist/health-cli.js"
  },
  "dependencies": {
    "@atlashq/jobs": "workspace:*",
    "@atlashq/logger": "workspace:*",
    "bullmq": "5.79.3",
    "pino": "10.3.1",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "24.13.2",
    "vitest": "4.1.10"
  }
}
